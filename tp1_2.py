from cgitb import text
from tp1 import *


def get_occurences(texte):
    occ
    for in range(0,len(texte))




def freq(texte):
    d = {}
    texte = del_space(to_miniscule(texte))
    occurences = []
    
    for c in occurences:
        if c not in d:
            if c not in car:
                d[c.lower()] = 1
        else:
            d[c.lower()] += 1
            
    return d


